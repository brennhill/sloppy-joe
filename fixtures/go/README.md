# Go Fixtures

- `go-sum-pass`: external module with `go.sum`
- `missing-go-sum-fail`: external module without `go.sum`
- `local-replace-pass`: local replace target that currently fails until Go local provenance is modeled explicitly
