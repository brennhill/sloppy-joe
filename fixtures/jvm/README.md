# JVM Fixtures

- `gradle-pass`: Gradle project with `gradle.lockfile`
- `maven-warning`: Maven project that should continue with a warning because there is no standard lockfile
- `custom-repo-fail`: Gradle custom repository that must fail closed
